#data structure
  #rows - observer/scenario
  #col - observed codes/behaviors
#min/max
  #provide choice for one or multiple min/max scenarios
  #uniform, max = c(0.95) or c(0.95, 0.9, 0.85)
  #more complex scenarios
    #very conservative
    #somewhat conservative
    #somewhat optimistic
    #very optimistic
#for each observer
  #name/id of each observer
  #if data missing for an observer, also include min/max
